# خطة إصلاح تشغيل الموقع على GitHub Pages

المشكلة الحالية هي توقف الموقع عن عرض البيانات عند رفعه على GitHub Pages رغم عمله محلياً. يرجع ذلك غالباً لقيود الأمان (CORS) أو فشل الاتصال بـ Supabase من نطاقات خارجية عند استخدام مفاتيح حساسة.

## Proposed Changes

### [Component Name] الواجهة الأمامية والاتصال بالبيانات

#### [MODIFY] [index.html](file:///D:/New%20folder/my/index.html)
سأقوم بتعديل دالة `loadWebsiteData` لتكون أكثر مرونة (Resilient):
1.  **إضافة نظام الاسترداد الاحتياطي (Fallback):** في حال فشل الاتصال بـ Supabase (وهو ما يحدث الآن في GitHub)، سيقوم النظام تلقائياً بمحاولة جلب البيانات من ملفات JSON المحلية المتاحة في المشروع (`data.json`, `vsa.json`, `technical-analysis.json`).
2.  **تحسين التعامل مع الأخطاء:** إضافة رسائل توضيحية في "الكونسول" لمعرفة أي ملف بالضبط فشل تحميله.
3.  **تصحيح المسارات:** التأكد من أن جميع عمليات `fetch` تستخدم مسارات نسبية متوافقة مع GitHub Pages.

## Verification Plan

### Manual Verification
- سأطلب منك رفع الملف المعدل إلى GitHub وتجربة الموقع.
- التحقق من ظهور البطاقات في الأقسام المختلفة (التحليل الحجمي، الاستثمار، إلخ).
- فحص "تنبيهات الكريبتو" للتأكد من أنها تجلب البيانات من `crypto_alerts.json` بنجاح.
