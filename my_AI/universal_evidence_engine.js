/** Universal Evidence Engine 1.1 - proof, acceptance, confidence and checkpoints. */
const UNIVERSAL_EVIDENCE_ENGINE_VERSION = '1.1-evidence';
function ueArray(v){return Array.isArray(v)?v:[];}
function ueString(v){return typeof v==='string'?v:'';}
function ueStatus(v){if(v==null)return'unknown';if(typeof v==='boolean')return v?'pass':'fail';const s=String(v).toLowerCase();if(/pass|success|ok|healthy|verified|complete|accepted|resolved/.test(s))return'pass';if(/fail|error|blocked|unhealthy|rejected|rollback/.test(s))return'fail';return'unknown';}
function ueSnapshotSummary(s){if(!s||typeof s!=='object')return{available:false,fileCount:0,componentCount:0};const files=Array.isArray(s.files)?s.files:(s.fileMap&&typeof s.fileMap==='object'?Object.keys(s.fileMap):[]);const components=Array.isArray(s.components)?s.components:(Array.isArray(s.architecture?.components)?s.architecture.components:[]);return{available:!s.error,fileCount:files.length,componentCount:components.length,framework:s.framework||s.language||s.architecture?.framework||null,platform:s.platform||s.architecture?.platform||null};}
function ueFingerprint(snapshot,normalizer){try{const v=typeof normalizer==='function'?normalizer(snapshot||{}):(snapshot||{});const j=JSON.stringify(v);let h=2166136261;for(let i=0;i<j.length;i++){h^=j.charCodeAt(i);h+=h*16777619;}return(h>>>0).toString(16);}catch(_){return'';}}
function ueCriterion(id,ok,required,reason,evidence=null){return{id,required:required!==false,status:ok===true?'pass':ok===false?'fail':'unknown',reason:reason||'',evidence};}
function ueVerificationStatus(result){const v=result?.verification||result?.closedLoop?.verification||{};return ueStatus(v?.status??v?.overall?.status??v?.result??result?.closedLoop?.status);}
function ueEvaluateMission(options={},result={},baseline=null,post=null,normalizer=null){const verify=options.verify!==false,implementationChanged=Boolean(result?.implementation?.changed),verificationStatus=ueVerificationStatus(result),baselineFingerprint=ueFingerprint(baseline,normalizer),postFingerprint=ueFingerprint(post,normalizer),architectureChanged=Boolean(baselineFingerprint&&postFingerprint&&baselineFingerprint!==postFingerprint);const custom=ueArray(options.acceptanceCriteria);const criteria=custom.length?custom.map((entry,i)=>{const c=typeof entry==='string'?{id:`custom_${i+1}`,type:entry,required:true}:entry;switch(c.type){case'implementation_changed':return ueCriterion(c.id,implementationChanged,c.required,implementationChanged?'Implementation change proven.':'No implementation change proven.',{implementationChanged});case'architecture_changed':return ueCriterion(c.id,architectureChanged,c.required,architectureChanged?'Pre/post architecture differs.':'No architecture difference proven.',{baselineFingerprint,postFingerprint});case'verification_pass':return ueCriterion(c.id,verificationStatus==='pass',c.required,`Verification status: ${verificationStatus}.`,{verificationStatus});case'post_discovery':return ueCriterion(c.id,Boolean(post&&!post.error),c.required,post&&!post.error?'Post-change discovery completed.':'Post-change discovery failed.',ueSnapshotSummary(post));case'runtime_pass':{const r=result?.verification?.runtime||result?.verification?.runtimeChecks||{};const s=ueStatus(r?.status??r?.result??r?.passed);return ueCriterion(c.id,s==='pass',c.required,`Runtime status: ${s}.`,{runtimeStatus:s});}case'api_pass':{const a=result?.verification?.api||result?.verification?.apiTesting||{};const s=ueStatus(a?.status??a?.result??a?.passed);return ueCriterion(c.id,s==='pass',c.required,`API status: ${s}.`,{apiStatus:s});}default:return ueCriterion(c.id,null,c.required,`Unsupported criterion type: ${c.type}.`);}}):[ueCriterion('objective_defined',Boolean(ueString(options.objective).trim()),true,'Mission objective is explicit.'),ueCriterion('architecture_plan',Boolean(result?.plan),true,result?.plan?'Architect plan returned.':'No architect plan returned.'),ueCriterion('verification_evidence',!verify||verificationStatus==='pass',true,verify?`Verification status: ${verificationStatus}.`:'Verification disabled.'),ueCriterion('post_discovery',Boolean(post&&!post.error),true,post&&!post.error?'Post-change discovery completed.':'Post-change discovery failed.',ueSnapshotSummary(post)),ueCriterion('no_unresolved_failure',ueStatus(result?.status)!=='fail',true,`Loop status: ${ueStatus(result?.status)}.`)];const required=criteria.filter(c=>c.required),failed=required.filter(c=>c.status==='fail'),unknown=required.filter(c=>c.status==='unknown');let verdict='partial';if(ueStatus(result?.status)==='fail'||verificationStatus==='fail'||failed.length)verdict='failed';else if(!unknown.length&&(!verify||verificationStatus==='pass'))verdict='accepted';let score=(baseline&&!baseline.error?15:0)+(result?.plan?20:0)+(implementationChanged?15:0)+(verify&&verificationStatus==='pass'?25:(!verify?10:0))+(post&&!post.error?15:0)+(architectureChanged?10:0);score=Math.max(0,Math.min(100,score));if(verdict==='failed')score=Math.min(score,35);else if(verdict==='partial')score=Math.min(score,79);const reasons=[];if(verdict==='accepted')reasons.push('All required evidence gates passed.');if(!implementationChanged)reasons.push('No implementation change was proven.');if(verify&&verificationStatus!=='pass')reasons.push(`Functional verification is not proven (${verificationStatus}).`);if(!post||post.error)reasons.push('Post-change discovery is missing or failed.');if(unknown.length)reasons.push(`${unknown.length} required criterion(s) are unknown.`);return{verdict,confidence:{score,level:score>=85?'high':score>=60?'medium':'low',reasons},criteria,facts:{baselineFingerprint,postFingerprint,architectureChanged,verificationStatus,implementationChanged}};}
function createUniversalEvidenceMission(options={}){const missionId=options.missionId||`mission-${Date.now().toString(36)}`;return{missionId,version:UNIVERSAL_EVIDENCE_ENGINE_VERSION,state:'created',phase:'preflight',objective:ueString(options.objective).trim(),environment:options.environment||'unknown',startedAt:new Date().toISOString(),checkpoint:null,evidence:[],acceptance:null};}
function finalizeUniversalEvidenceMission(mission,options,result,baseline,post,normalizer){if(!mission||typeof mission!=='object')throw new TypeError('mission must be an object');const acceptance=ueEvaluateMission(options,result,baseline,post,normalizer);mission.state=acceptance.verdict==='accepted'?'completed':acceptance.verdict==='failed'?'failed':'needs_review';mission.phase='finalize';mission.finishedAt=new Date().toISOString();mission.acceptance=acceptance;mission.evidence.push({phase:'baseline',status:baseline&&!baseline.error?'pass':'fail',summary:ueSnapshotSummary(baseline),fingerprint:acceptance.facts.baselineFingerprint},{phase:'implementation',status:result?.implementation?.changed?'pass':'unknown',changed:Boolean(result?.implementation?.changed)},{phase:'verification',status:acceptance.facts.verificationStatus},{phase:'post_discovery',status:post&&!post.error?'pass':'fail',summary:ueSnapshotSummary(post),fingerprint:acceptance.facts.postFingerprint},{phase:'verdict',status:acceptance.verdict,confidence:acceptance.confidence});mission.checkpoint={missionId:mission.missionId,state:mission.state,phase:mission.phase,verdict:acceptance.verdict,confidence:acceptance.confidence,updatedAt:mission.finishedAt};return mission;}
if(typeof module!=='undefined')module.exports={UNIVERSAL_EVIDENCE_ENGINE_VERSION,ueArray,ueString,ueStatus,ueSnapshotSummary,ueFingerprint,ueEvaluateMission,createUniversalEvidenceMission,finalizeUniversalEvidenceMission};
