name: Benchmark Integration Runner
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute integration and behavioral gate
        run: node .github/skill_benchmark_integrate.js
